name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: npm update
      run: npm update
    - name: npm run build
      run: npm run build
    - name: deploy
      run: |
        cp -r ./dist ./
